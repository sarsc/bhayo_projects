<h1>PROJECT</h1>

<div>
  <div class="slider">
  <% @project.images.each do |image| %>
    <div> <%= cl_image_tag image.photo_url %></div>
  <% end %>
  </div>
</div>
<div>
  <%= @project.name %>
  <%= @project.client %>
</div>

<%= link_to "edit", edit_project_path %>
